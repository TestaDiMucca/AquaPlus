{
  "name": "aquaplus",
  "version": "1.0.0",
  "description": "The premiere useless local streaming application.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently --names 'AQUA,KAZUMA' --prefix-colors 'red,yellow' \"cd client && npm run dev\" \"cd server && npm run dev\""
  },
  "author": "jkwok0714",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
